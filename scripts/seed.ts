/**
 * ローカル D1 にシードデータを投入するスクリプト
 * 使い方: npx wrangler d1 execute shirankedo --local --file=scripts/seed.sql
 */

// このファイルは seed.sql を生成するためのテンプレート
// 実際の投入は seed.sql を使う

console.log(
  "seed.sql を使ってください: npx wrangler d1 execute shirankedo --local --file=scripts/seed.sql",
);
